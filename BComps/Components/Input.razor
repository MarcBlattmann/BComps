<input class="@Version.ToString()" placeholder="@PlaceHolder"/>

@code {
    public enum Ver {
        Primary,
        Secondary
    }
    
    [Parameter, EditorRequired] 
    public Ver Version { get; set; }
    
    [Parameter]
    public string? PlaceHolder { get; set; }
}